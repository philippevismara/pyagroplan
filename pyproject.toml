[build-system]
requires = ["setuptools >= 70.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pyagroplan"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "matplotlib",
  "numpy",
  "pandas>=2.1.0",
  "networkx",
  "pychoco==0.2.1",
]
